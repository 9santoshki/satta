<!-- make build           # Builds and runs the containers
make up              # Starts the containers in detached mode
make down            # Stops and removes containers
make restart         # Restarts the containers
make logs-backend    # Tail logs for the backend service
make logs-frontend   # Tail logs for the frontend service
make migrate         # Apply Django migrations
make createsuperuser # Create a superuser for Django admin
make status          # Check the status of the containers
make prune           # Clean up unused Docker containers/images -->
